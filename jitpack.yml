# JitPack configuration
# https://jitpack.io/#m00nsolutions/m00n-junit5-reporter

jdk:
  - openjdk21

# Build the reporter module
install:
  - ./gradlew :m00n-junit5-reporter:publishToMavenLocal -x test

# Make gradlew executable
before_install:
  - chmod +x gradlew
